<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="currentWeatherTextSmall">
        <item name="android:textSize">@dimen/current_small_text_size</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">25dp</item>
    </style>

    <style name="list_column_container">
        <item name="android:layout_gravity">center</item>
        <item name="android:orientation">vertical</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="hour_container" parent="list_column_container">
        <item name="android:layout_width">@dimen/hour_width</item>
    </style>

    <style name="small_value_container" parent="list_column_container">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="list_text">
        <item name="android:gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/list_textcolor</item>
    </style>

    <style name="list_hour_text" parent="list_text">
        <item name="android:textSize">@dimen/list_hour_textsize</item>
    </style>

    <style name="list_small_value" parent="list_text">
        <item name="android:textSize">@dimen/list_small_value_size</item>
    </style>

    <style name="list_label" parent="list_text">
        <item name="android:textSize">@dimen/list_label_size</item>
    </style>

    <style name="list_temp_value" parent="list_text">
        <item name="android:layout_gravity">center_vertical</item>
        <item name="android:gravity">right</item>
        <item name="android:textSize">@dimen/list_temp_textsize</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_width">80dp</item>
    </style>

</resources>
