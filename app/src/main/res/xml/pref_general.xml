<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="0"
        android:entries="@array/pref_speed_unit_titles"
        android:entryValues="@array/pref_speed_unit_values"
        android:key="settings.speed.unit"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_speed_unit" />

    <ListPreference
        android:defaultValue="0"
        android:entries="@array/pref_temperature_unit_titles"
        android:entryValues="@array/pref_temperature_unit_values"
        android:key="settings.temp.unit"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_temperature_unit" />

</PreferenceScreen>
